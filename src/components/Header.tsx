'use client';

// import NavBar from './NavBar';

export default function Header() {
  return (
    <header className="fixed w-full top-0 start-0 z-50 bg-[var(--header-background)]">
      {/* <NavBar /> */}
      <p className="text-xl">Hello</p>
    </header>
  );
}
